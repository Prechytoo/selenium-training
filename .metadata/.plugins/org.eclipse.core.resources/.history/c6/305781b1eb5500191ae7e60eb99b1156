import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class Css {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		//xpath - //tagname[@attribute='value']
		//css-  [class='value']
		
		//when using firepath in fire fox to find a unique element or to find element
		//WHEN THERE IS ID PUT # AND IF IS A CLASS PUT A DOT .
		//REMOVE A SPACE
		
		WebDriver driver = new firefoxDriver();
		driver.get("https://login.saleforce.com/");
		driver.findElement(By.cssSelector("[class='identity first']")).sendKeys("HELLOW"); //CSS
		driver.findElement(By.cssSelector("input[id='password']")).sendKeys("123456");
		driver.findElement(By.xpath("//button[@id='Login']")).click(); //xpath
		driver.findElement(By.cssSelector("login"));
		
		
		
		

	}

}
